# Claude Code 実装プロンプト — 会話型AI見積もりツールLP

添付する最新デザイン画像と、以下の仕様をもとに本番実装してください。画像と仕様に差異がある場合は、以下の仕様（特に地域別LP、比較セクション、WHY US）を正とします。画像を単に貼るのではなく、HTML/CSS/Reactコンポーネントとして忠実に再現してください。

## 目的
地方・地域企業の事業会社に対して、業務上の困りごとを起点に無料見積もり／無料相談を獲得し、初期需要を検証するLPを作る。ユーザーに「欲しいシステム」を説明させず、「困っている仕事」を答えてもらい、必要な仕組み・概算費用・画面案へつなげる。

## 技術方針
- 既存リポジトリのフレームワーク、パッケージマネージャ、ディレクトリ構成を最優先で維持する。
- React/Next.jsの場合はApp Router前提。TypeScriptを使用する。
- 不要なUIライブラリは追加しない。CSS Modulesまたは既存CSS方式で実装する。
- セマンティックHTML、キーボード操作、適切なlabel、focus-visible、prefers-reduced-motionに対応。
- 主要ブラウザの現行版、375px〜1440px以上に対応。
- Lighthouseを意識し、CLSを出さず、画像がある場合は寸法を明示する。

## デザインシステム
デザイン参照サイトは別途指定されたサイトとし、添付画像の見た目を最優先する。参照元のブランド名やロゴは実装へ持ち込まない。
- 色: navy #071b34 / ink #0c233c / cobalt #1e49e2 / cyan #76d2ff / surface #f5f7fc / white。
- 日本語は Noto Sans JP、英字ラベルは DM Sans。未読込時のフォールバックも指定。
- 大見出しは太すぎない600、letter-spacing:-0.05em前後、広い余白、細い罫線。
- 角丸は原則0〜2px。ピル型は補助タグのみ。過剰な影、オレンジ、グラデーション過多を避ける。
- セクション幅は最大1240px、PC左右40px、SP左右16px。
- 添付画像のFV、濃紺セクション、白セクション、FAQ、最終CTA、フッターの比率と余白を正確に再現する。

## ページ構成（ストーリーテリング型LPの定石）
1. 濃紺ヘッダー: 会話型AI見積もり / CONVERSATIONAL AI ESTIMATE、主要セクションへ移動する「相談でわかること / 課題 / 解決方法 / 進め方 / 他との違い / よくある質問」と「無料で相談する」。フッターにも同じページ内メニューを置く。
2. FV: 左に地域・業種別ラベル、共通のメインメッセージ「人が辞めても、仕事が止まらない会社へ。」、右に無料業務整理・概算相談フォーム。説明文は「業務の話を約30分聞かせてください。AIと人で、任せられる仕事・最初につくる仕組み・概算費用を無料で整理します。」。フォームは初期ビューポート内に収める。
3. 無料相談でわかること: FV直下に「どこで仕事が止まるか / 最初に仕組みにする仕事 / 費用の目安」の3点を、番号付きの大きなカードで提示する。
4. 業種別のよくある相談: 薄青の独立セクションにする。地域・業種別の代表業務を「01〜05」の大きな番号付きカードで見せる。
5. 課題提起: 「こんな仕事が、人に頼りきりになっていませんか？」。誇張せず、担当者の経験に依存しやすい日常業務を具体化する。
6. 影響: 「仕事が特定の人に残り続けます。」を、返事が遅れる／引き継げない／改善が進まない、という実務上の3つの影響で説明する。恐怖訴求を強くしすぎない。
7. 解決方法: 「まずは、困っている仕事を聞かせてください。」。「何を作るか」が決まっていなくてもよい、と明示する。PPT由来の画面イメージを `public/ai-image1.png`（AIヒアリング）と `public/ai-image2.png`（概算結果）として2枚表示し、続けて業種別に「相談内容 → 最初の提案」を一例として表示する。
8. 3ステップ: 「ご相談から提案までの流れ」として、困りごとを教える / AIと人が整理 / 概算と画面案。
9. 市場比較: 表ではなく「既存3類型 → それぞれの限界 → 私たちが埋める空白」の流れで見せる。3類型は「見積もりSaaS」「AI開発サービス」「簡易見積もりLP」。
10. ご相談で大切にすること: 濃紺の全面セクションにする。左に「業務に合う形を、一緒に考えます。」、右に大きな01〜03と「費用は、幅を持ってお伝えします。」「人が、現場に合うかを確認します。」「小さな仕事から始められます。」を並べる。
11. ご相談の進め方: 約30分のヒアリング、できることの整理、内容を見て進め方を決められることを3カードで説明。仕様書不要、開発依頼は必須でないこと、詳しい業務の共有前にはNDAを含めて進め方を相談できることを明記。運営会社名とオンライン全国対応もここへ置く。
12. FAQ 4問。「ご相談の前に」としてdetails/summaryで実装。
13. 濃紺の最終CV: 課題がまとまっていなくても相談できることを再訴求し、フォームへ戻す。最後にフッター。

## FVフォーム
- 選択肢: 見積もり作成に時間 / Excel・紙・FAXを減らしたい / 人によってやり方が違う / 既存システムが使いづらい / その他。
- その他選択時だけ詳細textareaを表示。
- 会社メール必須、type=email。
- 送信時に実APIが未確定なら、送信ハンドラーを差し替えやすく分離し、成功状態を画面内表示。契約成立扱いにしない。
- UTMと可変ターゲット値をhiddenで送れるようにする。
- CTA文言は「無料で業務と費用感を整理する」。営業電話なし、送信で契約にならない旨を明記。

## 地域・ターゲット可変
トップ `/` は汎用LPとして残す。個別LPは `/lp/[region]/[target]` で作る。**FVのメインメッセージ、説明文、フォームは必ず本丸LPと同一にし、FV上部の「地域 × 業種・業態の業務改善」ラベルだけを差し替える。** FV以降の相談例、課題、最終CTAは業種ごとに差し替える。広島向けに下記5本を実装する。
- `/lp/hiroshima/manufacturing`: 製造業。「見積もりが、できる人待ちにならない工場へ。」図面確認・原価計算・見積書作成。
- `/lp/hiroshima/construction`: 建設・設備工事。「現場が忙しくても、見積もりを待たせない会社へ。」見積積算・現場写真・協力会社との連絡。
- `/lp/hiroshima/wholesale`: 卸売業。「FAXと電話の受注を、次の担当者へ渡せる仕事に。」受注転記・在庫確認・見積書作成。
- `/lp/hiroshima/logistics`: 物流・運送業。「電話と紙に埋もれる配車を、止まらない仕組みに。」配車調整・運行報告・請求確認。
- `/lp/hiroshima/care`: 介護・福祉。「利用者に向き合う時間を、記録と確認に奪わせない。」ケア記録・申し送り・シフト・請求確認。
- `/lp/hiroshima/travel`: 旅行業。旅行見積もり・行程表作成・手配状況の確認・お客様への連絡・精算／請求確認。

京都向けにも同じ5業種を追加する。FV本文は本丸LPと共通で、ラベルのみ「京都 × [業種・業態]の業務改善」とする。
- `/lp/kyoto/manufacturing`
- `/lp/kyoto/construction`
- `/lp/kyoto/wholesale`
- `/lp/kyoto/logistics`
- `/lp/kyoto/care`
- `/lp/kyoto/travel`

旅行業向けでは、問い合わせ対応・旅行見積もり・行程表作成・手配状況の確認・精算／請求確認を代表的な相談例として表示する。
必要であれば `?target=` も同じ設定を参照してプレビュー切替に使えるようにする。未知値はdefaultへ安全にフォールバックする。地域・業種ごとに、汎用版の文言をただ置換するのではなく、固有の「困る仕事」と「良くなる状態」を明確にする。文言変更でレイアウトが崩れないこと。

## フローティングCTA（参照サイトと同じ挙動）
- 600pxスクロール後に右下からフェード＋16pxスライドで表示。
- PC: 濃紺コンテナ内に「3分・入力無料 / 無料見積もり」と「オンライン対応 / 無料相談 →」の2導線。どちらもFVフォームへsmooth scroll。
- スクロール中、900ms以上間隔を空けて1.09倍へ240msだけ脈動。transform-originは右下。
- prefers-reduced-motionでは脈動を無効化。
- footerがIntersectionObserverで見えたら退避。
- 閉じるボタンあり。閉じたらsessionStorageに保存し、そのセッション中は再表示しない。
- SP（720px以下）は画面下部100%幅。脈動拡大なし。safe-area-inset-bottom対応。
- focus-visible、aria-label、印刷時非表示。

## SEO
- title: 無料AI見積もり｜困りごとから業務ツールをご提案
- description: 何を作るか決まっていなくても大丈夫。地域企業の困りごとを伺い、必要な仕組み・概算費用・画面イメージまで無料で整理します。
- canonical、OG、Xカード、Organization/ServiceのJSON-LDを実装。
- `public/og.png` をOG画像として利用。
- H1は1つ。見出し階層を正しくする。

## 品質確認
- desktop 1440×1000、tablet 768×1024、mobile 390×844で確認。
- FVのフォームが初期画面内にあり、CTAが見切れない。
- 横スクロール、文字切れ、意図しない改行がない。
- ラジオ、その他入力、メールvalidation、成功状態、FAQ、アンカー、可変target、フローティングCTAの出現/脈動/閉じる/フッター退避をテスト。
- build、lint、型チェックを通す。
- 最後に変更ファイル一覧、検証結果、未接続の外部APIだけを簡潔に報告する。

禁止事項: 添付画像の画像貼り付けによる再現、Lorem ipsum、架空の導入実績や数値、強制的な営業同意、過度なアニメーション、巨大な依存追加。
